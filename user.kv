#:kivy 2.1.0

<UserScreen>:
    MDBoxLayout:
        orientation: "vertical"

        MDTopAppBar:
            title: "MegaLan"
            left_action_items: [["menu", lambda x: app.callback(x), "Menu"]]
            elevation: 10

        MDLabel:
            text: "Content"
            halign: "center"
